# Training Screenshots

Place the screenshots captured during your manual LlamaFactory training session in this directory:

1. **`training_config.png`**: Capturing the WebUI setup page with the model, LoRA, and curated dataset selected.
2. **`loss_curve.png`**: Capturing the training loss descent chart generated after SFT completes.

*These images are required by the assignment guidelines and must represent actual training runs (no fabricated images).*
